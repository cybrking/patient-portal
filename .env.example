# Copy this file to .env and fill in values before running docker-compose.
# NEVER commit .env to version control.

# PostgreSQL password — use a randomly generated string of at least 32 characters.
# Example (Linux/macOS): openssl rand -hex 32
POSTGRES_PASSWORD=

# Redis auth token — use a randomly generated string of at least 32 characters.
# Example: openssl rand -hex 32
REDIS_PASSWORD=

# JWT signing secret — use a randomly generated string of at least 64 characters.
# Example: openssl rand -hex 64
# NOTE: For production, migrate to RS256 with the private key stored in AWS KMS
# and set JWT_SECRET_KEY from AWS Secrets Manager instead.
JWT_SECRET_KEY=
